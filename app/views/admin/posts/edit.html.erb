<div class="row">

    <div class="col-md-12">
      <div class="card card-plain">
        <%= link_to 'Show', admin_post_path(@post), {:class => 'btn btn-default'} %>
        <%= link_to 'Back', admin_posts_path, {:class => 'btn btn-default'} %>
      </div>

      <div class="card">
          <div class="header">
              <h4 class="title">New Post</h4>
              <p class="category">Create new post</p>
          </div>
          <%= render 'form' %>
      </div>
    </div>

</div>